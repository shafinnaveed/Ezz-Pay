<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">
    <View
        android:id="@+id/topBackgroundView"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="-1dp"
        android:layout_marginTop="0dp"
        android:layout_marginEnd="0dp"
        android:layout_weight="1"
        android:background="@drawable/gradient_background"
        />
    <TextView
        android:textColor="@color/black"
        style="@style/TextViewStyle"
        android:id="@+id/transactionLimitText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:layout_centerHorizontal="true"
        android:text="Get instant Loan of 5000 and pay back 5250 within 30 days"
        android:textSize="16sp" />
    <Button
        android:id="@+id/requestLoanButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Instant loan"
        android:layout_marginTop="30dp"
        android:layout_below="@id/topBackgroundView"/>
    <Button
        android:id="@+id/payBackLoanButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Pay-back loan"
        android:layout_marginTop="30dp"
        android:layout_below="@id/requestLoanButton"/>

</RelativeLayout>